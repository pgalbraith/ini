; The same entry as in foo.ini: a value with embedded newlines that cannot
; be written as indented continuation lines, so encode() JSON-quotes it and
; strictMultiline rejects it.

; wrap in quotes to JSON-decode and preserve spaces
" xa  n          p " = "\"\r\nyoyoyo\r\r\n"
